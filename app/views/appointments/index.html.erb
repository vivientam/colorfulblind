<h3>Your appointment</h3>

<% if @appointments %>
  <p class="text-general text-reading">
    You don't have any appointments yet...
  </p>
<% else %>
  <div class="table-responsive">
    <table class="table table-striped table-text">
      <thead>
        <tr>
          <th>#</th>
          <th>First Name</th>
          <th>Last Name</th>
          <th>Email</th>
          <th>Date of Appointment</th>
          <th>Date of Create</th>
          <th>Link</th>
        </tr>
      </thead>
      <tbody>
        <% @appointments.each do |appointment| %>
          <tr>
            <td><%= appointment.id %></td>
            <td><%= appointment.current_user.first_name %></td>
            <td><%= appointment.current_user.last_name %></td>
            <td><%= appointment.current_user.email %></td>
            <td><%= appointment.date %></td>
            <td><%= appointment.created_at %></td>
            <td><%= link_to "Link to this appointment", appointment_path(appointment.id), :class => "btn btn-xs btn-info" %></td>
          </tr> 
        <% end %>
      </tbody>
    </table>
  </div>
<% end %>
